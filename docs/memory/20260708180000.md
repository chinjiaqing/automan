# 工作总结 2026-07-08

## 本次完成事项

1. 创建 `docs/` 目录
2. 新建 `docs/开发前必读.md`，编写项目基础开发约束文档，共 12 个章节

## 变更文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `docs/开发前必读.md` | 新建 | 项目基础开发约束，共 12 节 |

## 文档章节概览

| # | 章节 | 核心内容 |
|---|------|---------|
| 1 | 语言规范 | 严格 TypeScript，禁 `any` 滥用 |
| 2 | 项目目录划分 | `server/` / `web/` / `shared/` 三层职责与依赖方向 |
| 3 | 模块系统 | ESM，禁 `require`，导入路径带扩展名 |
| 4 | 代码风格 | Prettier、命名约定、文件行数限制 |
| 5 | WebSocket 约束 | 禁在 route 写 WS，三层分层 |
| 6 | Actor 约束 | 状态隔离、禁 `while(true)`、完整生命周期 |
| 7 | 异步与错误处理 | `async/await`、禁空 catch |
| 8 | 日志规范 | 统一 Logger，禁裸 `console.log` |
| 9 | Device 层约束 | Actor 不直接操作设备 |
| 10 | 提交规范 | 类型检查 + commit message 格式 |
| 11 | 包管理 | 统一 pnpm，禁 npm/yarn |
| 12 | 工作汇报 | 每次完成后写 `docs/YYYYMMDDHHmmss.md` |

## 关键决策

- `shared/` 严格禁止依赖 `server/` 和 `web/`，保证前后端共用的代码无副作用
- 工作汇报由执行者主动撰写，不需额外提醒，形成闭环

## 下一步计划

- 初始化 `package.json`，配置 ESM + TypeScript + Prettier
- 搭建 `server/` / `web/` / `shared/` 目录骨架
- 实现 Fastify 应用入口及 WebSocket 插件基础结构
